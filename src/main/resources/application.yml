spring:
  docker:
    compose:
      enabled: true
      file: src/main/resources/docker-compose.yaml
  data:
    mongodb:
      host: mongodb://ms-agenda-mongo:27017
      port: 27017
      database: agenda
      username: root
      password: root